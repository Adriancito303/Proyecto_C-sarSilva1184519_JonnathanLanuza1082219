@model IEnumerable<Proyecto_CésarSilva1184519_JonnathanLanuza1082219.Models.Patients>

@{
    ViewData["Title"] = "Index";
}

<h1>Wait List for Vaccination COVID-19 / 2021</h1>

<p>
    <a asp-action="Create">Make an appointment</a>
</p>
<p>
    <a asp-action="VaccinatedList">Vaccinated List</a>
</p>
<p>
    <a asp-action="xVaccinated">% of vaccinated</a>
</p>
<form method="post">
    <p style="color:#ff0000">USE ONLY ONE VALUE PER SEARCH </p>
    Search by Name: <input type="text" name="Name" value="@ViewData["SearchName"]" /></br>
    Search by LastName: <input type="text" name="LastName" value="@ViewData["SearchLastName"]" /></br>
    Search by DPI: <input type="text" name="DPI" value="@ViewData["SearchDPI"]" /></br>
    <input type="submit" asp-action="Search" />
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DPI)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.town)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DPI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.town)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
        }
    </tbody>
</table>
